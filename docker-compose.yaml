version: '3.8'

services:
  postgres:
    image: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: simagree
      POSTGRES_PASSWORD: simagree
      POSTGRES_DB: simagree

volumes:
  pgdata: